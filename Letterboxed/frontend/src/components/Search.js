import React from 'react';

const Search = () => (
  <div>
    <h2>Search Movies</h2>
    <input type="text" placeholder="Search..." />
    <button>Search</button>
  </div>
);

export default Search;
